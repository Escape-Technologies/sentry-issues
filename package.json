{
  "name": "sentry-issues",
  "displayName": "Sentry Issues",
  "description": "View sentry issues locally",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.96.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "sentry-issues.refresh",
        "title": "Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "sentry-issues.filter",
        "title": "Filter Projects",
        "icon": "$(search)"
      },
      {
        "command": "sentry-issues.resetApiKey",
        "title": "Reset Sentry API Key",
        "icon": "$(key)"
      },
      {
        "command": "sentry-issues.resetOrganization",
        "title": "Reset Sentry Organization",
        "icon": "$(organization)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "sentryIssuesSidebar",
          "title": "Sentry Issues",
          "icon": "assets/sentry.png"
        }
      ]
    },
    "views": {
      "sentryIssuesSidebar": [
        {
          "type": "tree",
          "id": "sentryIssuesSidebar.sidebar",
          "name": "Sidebar View"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "sentry-issues.filter",
          "when": "view == sentryIssuesSidebar.sidebar",
          "group": "navigation"
        },
        {
          "command": "sentry-issues.refresh",
          "when": "view == sentryIssuesSidebar.sidebar",
          "group": "navigation"
        },
        {
          "command": "sentry-issues.resetApiKey",
          "when": "view == sentryIssuesSidebar.sidebar",
          "group": "navigation@1"
        },
        {
          "command": "sentry-issues.resetOrganization",
          "when": "view == sentryIssuesSidebar.sidebar",
          "group": "navigation@2"
        }
      ]
    },
    "configuration": {
      "title": "Sentry Issues",
      "properties": {
        "sentry-issues.organization": {
          "type": "string",
          "default": "",
          "description": "Sentry Organization Slug"
        },
        "sentry-issues.url": {
          "type": "string",
          "default": "https://sentry.io",
          "description": "Sentry Instance URL"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "yarn run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "yarn run compile && yarn run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.96.0",
    "@typescript-eslint/eslint-plugin": "^8.17.0",
    "@typescript-eslint/parser": "^8.17.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^9.16.0",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}